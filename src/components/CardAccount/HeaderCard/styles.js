import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    icon_conta:{
        width: 30,
        height: 30,
        margin: 10
    },
    containerIconConta:{
        flexDirection: "row",
        alignItems: "center"
    },
});

export default styles;