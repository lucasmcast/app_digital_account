import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    containerCardTransfer:{
        alignItems: "center",
        margin:10,
        padding:5,
        width: 80
    },
    imageTransfer:{
        width: 20,
        height:20
    },
    textTransfer:{
        color: "#FFFFFF",
        fontSize: 10
    }
});

export default styles;