import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    containerCardSaldo:{
        margin: 10,
        width: "90%",
        height: 100,
        backgroundColor: "red"
    }
});

export default styles;